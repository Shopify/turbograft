language: ruby
rvm:
  - "2.0.0"
node_js:
  - "0.10"
before_script:
  - bundle install
  - npm install
  - npm install -g testem coffee-script
script: testem ci && cd test/example && bundle exec rake test
